{
  "name": "salepage-glow",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CMT#6 workshop — lead-capturing salepage wired to HubSpot + Stripe",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "stripe:listen": "stripe listen --forward-to localhost:3000/api/stripe-webhook"
  },
  "dependencies": {
    "stripe": "^17.0.0"
  }
}
